{
  "name": "dream-job-reality-check",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd .. && cp -r frontend/dist dist && echo 'Build output copied to root dist directory'"
  }
}
